# 邪修英语速成 · 设计文档 v1.0

> 日期：2026-09-17
> 定位：上班磨耳朵为主、碎片熟读为辅的英语基本盘工具
> 使用者：安装工艺工程师（海外现场配合、英文配音/字幕场景）
> 线上地址：https://mkeavan785-sudo.github.io/xiexiu-english/
> 仓库：https://github.com/mkeavan785-sudo/xiexiu-english （GitHub Pages，main 根目录 legacy 部署）

## 1. 目标与非目标

**目标**
- 零成本、零 API Key、零依赖：双击 index.html 即可运行
- 手机夸克浏览器 10.21 可用，手机端/PC 端 UI 相似度 >80%
- 部署 GitHub Pages，数据可导出带走
- 两条主线：自然拼读（见词能读）+ 词根词缀（成片扩词），口语现场句库磨耳朵

**非目标（v1 不做）**
- 不做语音识别打分（夸克/iOS 兼容差）
- 不做后端、不做账号、不做云同步
- 不做 PWA（列为 v1.1 候选：装桌面、离线、锁屏后台播放）

## 2. 四大模块

底部导航（手机）/ 顶部导航（PC），两端高亮同步，默认进入「磨耳朵」。

### 2.1 磨耳朵（核心，v2 改版）
- **词模式（独立场景词库，不接受外部喂入/导入）**：14 个逻辑场景 213 条高频实用词与小词组
  起床洗漱→早餐→出门通勤→到岗晨会→工地指挥口令→手动工具与操作→安装作业→午餐→问题处理→
  安全提醒→下班收工→晚间生活→海外出差机场→海外餐厅商店
  - 选词原则：外国人天天说的高频词，避开低级词与生僻词；允许 watch out / line up 类小词组
  - **顺序播放、不打乱**（按一天逻辑线教学），场景末自动进下一场景，全完循环回第一场
  - 进度持久化（immWords.scene/item），离开再进停在原处
  - 「从头开始」回第一场第一条；「下一组」跳到下一场景开头
- **句模式**：180 内置现场话术 + 我的导入句，中文→停顿→英文×N（1/2/3 遍），每轮乱序
- 语速 0.75 / 0.9 / 1.0 / 1.2；间隔 1 / 2 / 3 秒
- 控制：大播放键（≥56px）、上一个、从头开始、下一个、下一组、重听
- Wake Lock 保持常亮（可关）；切后台挂起由 v1.1 PWA 解决，v1 显式提示保持前台
- 必须由用户点击启动（满足浏览器自动播放策略）
- v2 已下线：词根喂养、我的单词导入（单词更新只在版本迭代中进 data-immersion.js）

### 2.2 熟读（自然拼读，M2）
- **完整版 46 规则组，7 阶段**（见 §4），每组词池 8–12 词
- **每日一阶段轮转**：每天自动推一个阶段（4–11 组），每组当天随机抽 3 词，约 5 分钟，7 天轮完
- 随机种子 = 日期，优先抽前两天未出现的词；阶段 0（字母）默认跳过，可设置打开
- 可手动选阶段加练，不影响主轮转
- 流程：听领读（单词喇叭 / 整组连读）→ 自己开口读 → 下一组。**无自评按钮**
- 完成当天阶段全部组 = 当日打卡，统计连续天数
- Tricky Words（约 80 个不规则高频词）不进熟读，直接进磨耳朵

### 2.3 词根库（M3）
- 50 个高频词根/词缀（port/spect/duct/stall/struct/pos/fer…），约 400 词
- 优先收录仓储安装行业词（conveyor/installation/maintenance/inspection/specification/automated 等约 120 个，标 ⭐）
- 每词展示：英文 / 中文 / 喇叭；**三级构词拆解 chips**（如 installation = in- 进入 + stall 放置 + -ation 名词后缀）
- v2：喂养链路下线，词根库为纯浏览/发音/拆解模块

### 2.4 我的（v2）
- 仅**句子导入**（配音稿/话术）：竖线或中英混排自动拆分，保留句末标点，去重
- 导出/导入 JSON 备份（单键 `xx_en_v1`）；统计概览；美/英音、字母阶段设置
- 单词不再外部导入，场景词库随版本迭代

## 3. 发音引擎（三级兜底）

1. **Web Speech API（speechSynthesis）**：优先 en-US（可切 en-GB），中文 zh-CN；处理 voices 异步加载、长句 10s resume 定时器、切换 cancel
2. **有道词典发音 URL**（`https://dict.youdao.com/dictvoice?audio=WORD&type=1`，type=0 英音）：TTS 不可用时**单词级**兜底，用 Audio 播放（需联网）
3. 两者均不可用：顶部横幅明确提示，绝不静默失败；句模式仅 TTS 可用，降级时提示改用词模式

## 4. 自然拼读规则图谱（46 组）

- 阶段0 字母单音：26 字母（1 组，默认跳过）
- 阶段1 短元音 CVC（5）：a/e/i/o/u
- 阶段2 辅音组合（9）：sh、ch、th清、th浊、ck、ng、qu、wh、ph
- 阶段3 辅音连缀（4）：l家族 / r家族 / s家族 / 三辅音
- 阶段4 Magic E（5）：a_e/i_e/o_e/u_e/e_e
- 阶段5 长元音组合（6）：ai-ay / ee-ea-y / ie-igh / oa-ow-oe / ue-ew-oo长 / eigh-ey
- 阶段6 复合元音&r控制（11）：oo短、ou-ow、oi-oy、au-aw-al、ar、or、er-ir-ur、are-air、ear-eer、ore-oar、schwa
- 阶段7 特殊拼写（6）：软音c/g、dge-tch、词尾-le、哑音kn-wr-mb-gn、-tion/-sion、-ed三音/-s三音
- 附加：Tricky Words ≈ 80

## 5. 技术架构

- 纯静态：HTML + CSS + 原生 JS，**无框架、无构建、无 ES Module**（module 在 file:// 下失败），普通 `<script>` 挂全局
- localStorage 单键 `xx_en_v1`：settings / drill 快照（日期+抽词+打卡）/ fed 词根喂养表 / myWords / mySentences / stats
- 结构变化立即保存；设置项即时保存
- 安全区 env(safe-area-inset-*)；触摸目标 ≥44px（主按钮 ≥56px）；静态触屏检测（maxTouchPoints>0 且无精确指针）+ 触摸事件兜底
- 风格：暗底 + 琥珀 accent（与既有门户视觉统一）

```
邪修英语速成/
├─ index.html
├─ DESIGN.md
└─ assets/
   ├─ css/style.css
   └─ js/
      ├─ data-phonics.js    46组规则+约400拼读词（含IPA）+Tricky
      ├─ data-roots.js      50词根+约400词（行业⭐）         [M3]
      ├─ data-sentences.js  6场景约180句现场话术（中英）      [M2]
      ├─ audio.js           发音三级兜底 + Wake Lock
      ├─ store.js           localStorage + 导入导出
      ├─ immersion.js       磨耳朵播放器
      ├─ drill.js           熟读（每日阶段轮转）             [M2]
      ├─ roots.js / my.js   词根库 / 我的                    [M3/M4]
      └─ app.js             路由与导航
```

## 6. 里程碑

- **M1**：骨架导航 + 发音引擎 + 磨耳朵词模式（阶段0-2数据先行）→ 双击可听
- **M2**：句模式 + 熟读（阶段轮转 + 打卡）+ 全量 46 组数据 + Tricky + 现场句
- **M3**：词根库（展开 + 喂养）
- **M4**：✅ 我的词本（手动加词/中英混排粘贴批量导入/删除）、JSON 导出下载+文本兜底/文件恢复、统计概览、美英音与字母阶段设置；自定义词句实时并入磨耳朵
- **M5**：夸克实机测试 → 新建仓库 `xiexiu-english` → GitHub Pages
- **v1.1**：PWA（装桌面/离线/锁屏后台播放），视夸克验证结果决定

## 7. 内置内容体量（M2 实测）

- 46 组规则词原始条目 ≈ 578，跨组去重后 ≈ 530
- Tricky 不规则高频词 83 个
- 磨耳朵词模式总池（拼读词 + Tricky 去重）= **615 词**
- 现场话术 **180 句**（吊装指挥/安全/对位调整/确认听懂/进度协调/验收 6 场景，各 30 句）
- 词根库 50 个词根 / **298 词**（行业词约 100 个，标 ⭐），支持单词级与整族喂养磨耳朵、词根/单词/中文搜索
默认美音，设置可切英音。
